/**
 * Test stub for src/utils/ui.ts, which is renderer-only at module scope
 * (vue-router, history, refs). Node specs import src/utils modules (e.g.
 * memorizedTransactions) that only need routeTo from it.
 */
module.exports = {
  routeTo: async () => {},
  getFormRoute: () => '',
};
