/** Product copy — keep in sync with `index.html` boot splash markup. */
export const LIVEBOOKS_DESKTOP_TAGLINE =
  'Open source, offline-first accounting software powered by Frappe Books.';

export { LIVEBOOKS_DESKTOP_PRODUCT_NAME } from '../utils/livebooksAppEnv';
